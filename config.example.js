// Contentstack Configuration Template
// Copy this file to config.js and update with your actual credentials

window.CONTENTSTACK_CONFIG = {
    // Your Contentstack Stack API Key
    apiKey: 'your_stack_api_key_here',
    
    // Your Management Token (needs content type and entry permissions)
    managementToken: 'your_management_token_here',
    
    // Environment to publish to
    environment: 'development',
    
    // Content Type UID for MDU entries
    mduContentType: 'mdu_entries',
    
    // Content Type UID for Tariff entries (optional)
    tariffContentType: 'tariff_entries'
};